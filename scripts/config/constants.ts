
export const ID = {
    NOTIFICATION : {
        REPORT_LINK: "reportNotificationId"
    }
};

export const CLASS_PREFIX = "news-enhancer";


let templates = {
    article: "article.html",
    headline: "headlineInfo.html",
    submissionForm: "submissionForm.html",
    wordcloud: "wordcloud.html",
    modal: "modal.html",
};


export const HTML_FILES = {
    templates: templates,
    popup: "popup.html"
};

let icons = {
    logo: "icons/icon.png"
};

export const RESOURCES = {
    icons: icons
};

const MINUTE = 1000 * 60;

export const RELOAD_DELAY = 10 * MINUTE;