import {IMessage} from "../config/messageTypes";

/**
 * This will work as the controller for the message exchange between the content script and the other scripts.
 *
 */
class MessageHandler {
    sendMessage = (message: IMessage) => {
    };

}

export default MessageHandler;
